<view class="page">
  <view class="card">
    <text class="title">👋 填写信息，开始测试</text>
    <text class="subtitle">10~15 分钟量化孩子的听觉序列记忆广度与视觉空间工作记忆容量</text>

    <view class="field">
      <text class="label">学生姓名</text>
      <input class="input" value="{{name}}" placeholder="请输入姓名" maxlength="10" bindinput="onNameInput" />
    </view>
    <view class="field">
      <text class="label">所在学校</text>
      <input class="input" value="{{school}}" placeholder="请输入学校" maxlength="16" bindinput="onSchoolInput" />
    </view>
    <view class="field">
      <text class="label">手机号/微信号</text>
      <input class="input" value="{{phone}}" placeholder="请输入手机号或微信号" maxlength="20" bindinput="onPhoneInput" />
    </view>
    <view class="field">
      <text class="label">年龄</text>
      <input class="input" type="number" value="{{age}}" placeholder="请输入年龄" bindinput="onAgeInput" />
    </view>
    <view class="field">
      <text class="label">推荐老师工号（选填）</text>
      <input class="input" type="number" value="{{referrerTeacherId}}" placeholder="如有推荐老师，请输入其5位工号" maxlength="5" bindinput="onReferrerInput" />
    </view>

    <text wx:if="{{errorText}}" class="error-text">⚠️ {{errorText}}</text>

    <button class="btn btn-green" bindtap="startDual">🚀 开始双通道测评</button>
    <view class="solo-row">
      <button class="btn btn-solo" bindtap="startAuditory">🎧 仅测听觉</button>
      <button class="btn btn-solo" bindtap="startVisual">👁️ 仅测视觉</button>
    </view>

    <text class="privacy-note">🔒 信息仅用于生成本次测评报告</text>
  </view>
</view>

# 视听双通道综合专注力测评系统 - 项目交接文档 (HANDOFF)

> **文档创建时间**：2026-09-10  
> **项目名称**：清良思维 · 视听双通道综合专注力测评系统 (Dual-Channel Attention Assessment System)  
> **代码仓库**：[https://github.com/wizard9018/attention-test](https://github.com/wizard9018/attention-test)  
> **在线访问**：[https://wizard9018.github.io/attention-test/](https://wizard9018.github.io/attention-test/)  
> **当前分支**：`main` (与远程已同步)

---

## 一、 项目背景与核心架构

本系统是面向中小学生（K12）认知与专注力发展的专业双通道测评工具，融合神经心理学测试范式，评估学生在**听觉通道**和**视觉通道**的信息捕获、抗干扰与工作记忆复现能力。

### 1. 技术栈与设计特点
- **前端架构**：纯原生 HTML5 + CSS3 + ES6（轻量无框架，单文件开箱即用，高兼容性）。
- **音频系统**：92 个汉字字音全量升级为微软官方少年音（`zh-CN-YunxiaNeural`），编译为小于 1MB 的 Base64 离线离线包（`assets/audio_pack.js`），由 Web Audio API 首屏预解码并单例驻留内存，实现**零网络延迟（<2ms）发音**。
- **拼音算法库**：内置 `assets/pinyin-pro.js`，支持逆序拼音纠错与模糊容错。
- **后端网关（可选）**：Python `server.py`，支持 HTTP 206 Partial Content (Range) 音频流式分片及本地 SQLite 数据库持久化。
- **数据层**：
  - 本地 SQLite：`database/records.sqlite`（已被 `.gitignore` 保护，防止敏感数据泄露）。
  - 腾讯云 COS 存储：存储桶 `asr-1322227270`（`ap-beijing`）。
  - **待接入云端数据库**：**Supabase**（用户已选定方案，用于全网统一收集学生成绩）。

---

## 二、 近期完成的核心迭代与业务规则

1. **信息输入优化**：
   - 彻底移除“班级”字段，仅要求输入“姓名”和“学校”（以减轻信息填写负担并保护隐私）。

2. **听觉注意力测试流程**：
   - 正向朗读 3 字符、5 字符、6 字符，每字间隔严格控制为 1 秒；
   - 考生在输入框中**反向输入汉字或拼音**（如听“天-水-木”，反向输入“mu shui tian”或“木水天”）；
   - 自动拼音纠错与倒序复述判定。

3. **视觉空间注意力测试流程**：
   - **试答练习轮**：以 3 格作为试做题，不计入总分。答对平滑进入正式测试，答错可重复练习直到掌握规则。
   - **正式测试**：直接从第 5 级起跳（5 到 10 级梯度晋级）。
   - **三阶段范式**：
     - Stage 1（20s 观察记忆）：10×10 网格展示连通目标色块；
     - Stage 2（心算抗干扰）：口算 100 以内加减法算式（抗记忆衰退与反刍干扰）；
     - Stage 3（网格复原）：考生在空白 10×10 网格中点击还原目标位置。

4. **视觉对比视图（用户特别指定规范）**：
   - 彻底移除了“命中 / 错选 / 漏选”的色块区分与文字说明！
   - **左边**：`👤 左边：用户输入`，仅显示“已选 X 格”，忠实呈现用户点选的绿色色块，未选区保持空白；
   - **右边**：`🎯 右边：正确格子位置`，显示“目标共 N 格”，呈现原始蓝色目标块；
   - 纯净呈现，左右两图直观对称比对。

5. **评分与综合报告**：
   - 核心总分公式：$S_{总} = S_{听} \times S_{视}$（满分 120 分）；
   - 生成涵盖“听觉工作记忆、视觉空间复现、抗干扰稳定性、处理速度、抗焦虑指数、信息保真度”的六维认知雷达图与双通道发展报告。

6. **代码精简 (Simplify) 与代码审查 (Code Review)**：
   - 清理了调试阶段残留的所有冗余 `console.log`；
   - 增加了防御性的定时器安全清理 `clearInterval(visualTimerInterval)`；
   - 数据库连接升级为 Python `with get_db() as conn:` 上下文管理器；
   - 排除 800MB 本地大模型文件，Git 仓库体积极致压缩至 1MB 并推送到 GitHub。

---

## 三、 当前未完成任务：Supabase 云端数据库直连接入

### 1. 目标
在纯静态部署环境（如 GitHub Pages）下，学生直接在手机/电脑答题后，测试成绩能**实时自动写入 Supabase 云端数据库**，老师可在 Supabase 控制台统一查看、筛选并导出全班/全校成绩，无需运行本地 Python 服务器。

### 2. 待用户提供的信息（接力第一件事）
等待用户在 Supabase 后台获取并提供以下 2 个参数：
1. **`Project URL`**：形如 `https://xxxxxxxxxxxx.supabase.co`
2. **`anon (public) key`**：一段以 `eyJ...` 开头的公共 API Key

### 3. Supabase 建表 SQL（已准备好）
在 Supabase 的 **SQL Editor** 中执行以下脚本即可完成建表并开通免登录安全写入（RLS）：
```sql
-- 1. 创建学生专注力测试成绩表
create table if not exists student_attention_records (
  id bigint generated by default as identity primary key,
  player_name text not null,
  player_school text not null,
  auditory_score integer not null,
  visual_score integer not null,
  total_score integer not null,
  test_date text not null,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  details jsonb
);

-- 2. 开启安全策略 (Row Level Security)
alter table student_attention_records enable row level security;

-- 3. 允许学生前端匿名提交成绩 (Insert)
create policy "Allow public insert"
on student_attention_records
for insert
to anon
with check (true);

-- 4. 允许前端匿名读取排行榜成绩 (Select)
create policy "Allow public read"
on student_attention_records
for select
to anon
using (true);
```

### 4. 获取参数后的实施步骤
获得 URL 和 Key 后：
1. 在 `综合专注力测试.html` 和 `index.html` 的 `syncRecordToTencentCloud` 函数中，加入对 Supabase REST API 的直连调用：
   ```javascript
   fetch(SUPABASE_URL + "/rest/v1/student_attention_records", {
     method: "POST",
     headers: {
       "apikey": SUPABASE_ANON_KEY,
       "Authorization": "Bearer " + SUPABASE_ANON_KEY,
       "Content-Type": "application/json",
       "Prefer": "return=minimal"
     },
     body: JSON.stringify(payload)
   });
   ```
2. 在 `loadCloudLeaderboard()` 中从 Supabase 读取实时前 10 名排行榜；
3. 本地增加 `localStorage` 自动兜底缓存；
4. `git commit` 并 `git push` 到 GitHub，GitHub Pages 自动同步上线。

---

## 四、 新电脑环境搭建与快速启动

### 1. 代码获取
```bash
git clone https://github.com/wizard9018/attention-test.git
cd attention-test
```

### 2. 环境变量配置
复制模板文件创建本地凭据（若需要本地运行 Python 服务）：
```bash
copy .env.example .env
```
（并在 `.env` 中按需填入腾讯云或 Supabase 凭据，`.env` 已被 git 忽略，不会提交）。

### 3. 启动方式（支持任选一种）
- **方式 A：GitHub Pages 在线访问（推荐，零安装）**：
  直接访问：[https://wizard9018.github.io/attention-test/](https://wizard9018.github.io/attention-test/)
- **方式 B：本地双击启动**：
  Windows 下直接双击运行 `启动综合专注力测试.bat`。
- **方式 C：命令行本地运行**：
  ```bash
  python server.py
  # 浏览器访问: http://localhost:8080/index.html
  ```

---

## 五、 核心文件目录索引

```text
专注力/
├── HANDOFF.md                 # 【本文档】换机接力交接手册
├── index.html                 # GitHub Pages 默认入口 (综合测评生产版本)
├── 综合专注力测试.html          # 视听双通道综合专注力测评核心页面
├── 听觉专注力测试.html          # 单独听觉通道测试页面
├── 视觉专注力测试.html          # 单独视觉通道测试页面
├── server.py                  # 本地静态托管 + Range 音频流 + API 数据库网关
├── tencent_cloud_db.py        # 腾讯云 COS + 本地 SQLite 上下文连接模块
├── upload_to_tencent_cloud.py # 一键同步静态资源至腾讯云存储桶工具
├── .env.example               # 本地环境变量配置模板 (密钥脱敏)
├── .gitignore                 # Git 忽略配置 (屏蔽数据库、大模型、缓存)
├── assets/
│   ├── audio_pack.js          # 92 个汉字微软官方少年音 Base64 内嵌离线包 (<1MB)
│   ├── pinyin-pro.js          # 汉字转拼音与纠错算法库
│   ├── audio/chars/*.mp3      # 92 个汉字备用独立 MP3 音频文件
│   └── level_*.png            # 界面勋章与步骤说明图
└── database/                  # 本地数据库目录 (records.sqlite 运行时自动生成)
```

---

*文档已纳入版本控制，在新电脑上拉取仓库后可直接根据本指南继续无缝开发！*
